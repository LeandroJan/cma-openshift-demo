apiVersion: v1
kind: Service
metadata:
  name: springboot-demo
  namespace: demo-keda
  labels:
    app: springboot-demo
spec:
  selector:
    app: springboot-demo
  ports:
    - name: http
      port: 8080
      targetPort: 8080
